# InFlow English · 产品契约

完整、持续维护的产品裁决见 [`PRODUCT-SPINE.md`](PRODUCT-SPINE.md)。本文件保留中文速览。

## 公共承诺

用户打开带可用字幕轨的普通英语 YouTube 视频后：

```text
1 秒内看见 InFlow 状态
→ 不等待 localhost / yt-dlp / ASR / 模型
→ 中文主行 + 英文辅行
→ S 重听当前完整自然字幕片段
→ 失败在固定时限内结束，原视频继续
```

## 默认边界

- 自动字幕：默认开启；
- 自动教学暂停：默认关闭；
- 本机服务权限：安装时不申请，用户开启实验学习时才由 Chrome 询问；
- 广告：隐藏 InFlow 字幕，重置 8 秒门；
- SPA 换片：清空旧 generation，迟到结果不得覆盖新视频；
- 没有字幕：不启动重型学习；
- 长视频主动学习：正式环境关闭，字幕不受影响。

## 实验学习契约

用户明确 opt-in 后，连续、可见、非广告播放满 8 秒才允许后台准备。只有原声、句界、当前义项和暂停点全部 READY 才能暂停。

教学卡：

- 主操作：`看清了，继续`；
- 次操作：`再听一遍`；
- 可选抑制：`以后跳过此义项`，保存后可见且可撤销；
- 不强迫三态自评；
- 不自动消失；
- 用户 play/pause/seek/切页/广告会撤销恢复权。

## 证据语义

- 中文字幕辅助理解：`GIST_ONLY`；
- 一次教学：`TEACH`，低置信；
- 重听：独立行为计数；
- 普通继续：不写熟悉度；
- “以后不用解释”：可撤销 `EXPLICIT_KNOWN_OVERRIDE`；
- 延迟三选一正确：最多 `WEAK_SUCCESS`；
- 无事件自动写“掌握”。

## 发布停止线

以下任一成立就不能声称公开可用：状态不可见、无限加载、广告中显示/暂停、字幕串片、未授权自动暂停、旧 owner 被静默接管、正式数据被 QA 修改、ZIP 与源码同版本不同内容、或 Chrome Web Store 尚未批准却声称一键安装。
