# InFlow English · 延迟听音验证切片

## 产物身份

- 名称：`inflow-delayed-audio-probe-01`
- 身份：当前教学切片的相邻、可保留价值切片
- 目标：把“完成过教学”推进到“以后能否仅凭声音选出当前义项”的真实证据
- 不声称：一次答对等于掌握、同一合成声音等于跨说话人泛化、选择题等于自由回忆

## 用户路径

只有一个教过的词进入 `20–72 小时`验证窗口且尚未使用过当前验证材料时，下一次点击开始观看后：

```text
最多出现 1 题
→ 播放一条新句子，不显示英文词形和中文答案
→ 声音完整结束后显示 3 个中文义项
→ 用户点 1 次，或点“没听出来”
→ 立即显示英文词形＋正确中文义项，并重放新句子
→ 反馈一直保留到用户点击“开始观看”
```

若没有合格项目，直接进入视频。验证不在视频中途插入，也不增加新的长期设置。

## 证据边界

- 第一次完整播放新句后的作答才计入：
  - 正确选择：`WEAK_SUCCESS`。三选一有猜中概率，只增加一条延迟听觉辨认证据，不升级为掌握。
  - 错误选择：`RECOGNITION_FAILURE`。它只表示这一次三选一听音没有提取出目标义项；由于正确义项混在选项中，它不冒充完全无提示的自由回忆证据，并保留既往教学事实。
  - “没听出来”：`ABSTAINED_NONRETRIEVAL`，与选错分开记录；它可能是诚实避免猜测，不按负分处理。
- 刷新或接管后重听再作答：`ASSISTED_PRACTICE`，只累计辅助练习次数，不计入辨认成功或失败。
- 跳过、页面隐藏、音频未完整播放、技术失败、接管中断：`NOT_EVIDENCE`，profile 逐字段不变，也不训练强度偏好。
- 材料门槛：当前 16 项中仅 8 项通过题目级 QC 成为 `weak_pilot` 验证题；5 项待修正语境/语速，3 项结构上不适合作主测。未过 QC 的词照常参与教学，但不进入验证。
- 当前 followup 是不同于原视频的合成说话人与新句子，但每词只有一个材料版本；同一版本只测一次，不能重复刷成高置信状态。

## 选择与调度

- 每个观看 session 最多 1 题；验证与教学共享按有效英语语音计算的总介入预算；有题时，各档主动教学预算都减 1，不能借验证额外增加打扰；
- 只选 `taught` 且 `last_taught_at + 20h <= now <= last_taught_at + 72h` 的项目；
- 当前 `probe_variant_id` 已使用后不再出现；
- 错过窗口即沉默，不生成逾期任务；
- 本次被验证的词从同一 session 的主动教学候选中排除，避免答案刚揭示就再次打断；
- 题目选项固定为 3 个语义互斥中文义项，顺序由 session seed 决定；服务端保存正确映射，作答前 API 不下发英文词形、正确 ID 或反馈。

## 状态与可靠性

```text
probe_ready → probe → probe_feedback → watch
```

- `probe_id` 是唯一幂等键；同一结果重试只读回，不重复计数；同一 probe 出现冲突结果时重建直接失败。
- 音频必须完整播放，并以服务端确认的 `presentation_id` 登记；刷新后重听次数进入 `presentation_count`，第二次起的结果不再算首次辨认证据。
- 接管进行中的验证时，系统先在同一事务里把旧测量终结为技术故障，再转移 owner；迟到答案不能评分。
- 事件带有各自写入时的 `policy_version`；重放不会用新版本改写旧历史。
- 所有写入沿用 `owner_client_id + owner_epoch`；旧标签页不能作答或覆盖进度。
- probe、profile 与 event 使用同一可恢复 transaction；跳过与故障不触碰 profile。
- 刷新时未作答题可重新播放，不自动判错；反馈页刷新后仍能看到同一结果。
- reducer 按 `event_id` 和 probe 语义指纹双去重，并能从 `probe_completed` 重建画像。

## 验收

1. 未到 20h、超过 72h、没有教过、材料已用过、题目 QC 未过时完全不出现；
2. 到期时一场最多一题，且答案前无英文词形和正确中文义项泄露；
3. 未完整听完不能作答；
4. 正确、错误、没听出来、重听、跳过、技术失败分开保存；
5. 正确不把 `aural_stage` 写成 mastered/stable；
6. 反馈不自动退出，用户确认后才进入视频；
7. 刷新、重复提交、第二标签页、I/O 中断不重复计数；
8. QA 使用临时数据，正式 profile/session/event 前后不变。
